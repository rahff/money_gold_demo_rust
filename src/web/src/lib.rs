mod router;
mod controller;
mod exceptions;
mod transfer_gold_module;
pub mod app;
mod event_queue;
mod route_handler;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
